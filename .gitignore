# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Secrets (root .env has API keys — keep it ignored)
.env
# But frontend/.env is safe to commit (only VITE_API_BASE, no secrets)
!frontend/.env

# Generated artefacts (rebuild with: python -m app.ingestion.build_index)
data/chroma/
data/raw/
data/processed/
# Query telemetry — recreated on first request, and never worth committing.
data/analytics.sqlite3

# Frontend
node_modules/
frontend/dist/
*.local
frontend/.env

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/

# AI context files (not part of the project)
CLAUDE.md
.claude/
