import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    // The frontend always calls relative /api/... paths. In development this
    // proxy forwards them to the FastAPI backend; in production FastAPI serves
    // the built files itself, so the same URLs work with no code change.
    proxy: {
      "/api": {
        target: "http://127.0.0.1:8000",
        changeOrigin: true,
      },
    },
  },
  build: {
    outDir: "dist",
    emptyOutDir: true,
  },
});
